# API
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# LLM & Embeddings
openai==1.51.0

# Azure AI Search (production retrieval backend) + Managed Identity auth.
# Required for RETRIEVER_BACKEND=azure_ai_search and LLM_BACKEND=azure_openai.
azure-search-documents==12.0.0
azure-identity==1.25.3

# Vector Store (local backend only)
chromadb==0.4.24

# Sparse Retrieval (local backend only)
rank-bm25==0.2.2

# Reranker (local backend only — Azure uses the managed semantic ranker, so
# the Azure image installs requirements-azure.txt and omits everything below).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.0
sentence-transformers==3.1.1
# Pinned alongside sentence-transformers: unpinned transformers resolves to
# the latest 5.x-compatible release, whose meta-tensor model loading breaks
# sentence-transformers 3.1.1's CrossEncoder loading path at runtime.
transformers==4.44.2

# Document Ingestion
# Replace with:
#unstructured==0.15.13
pypdf==5.0.1
#python-docx==1.1.2

# Data Validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Evaluation
#ragas==0.2.1
#datasets==3.0.1

# Utilities
python-dotenv==1.0.1
tenacity==9.0.0
httpx==0.27.2
numpy==1.26.4
